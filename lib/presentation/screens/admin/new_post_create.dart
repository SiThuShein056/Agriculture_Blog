import 'package:flutter/material.dart';

class NewPostCreate extends StatelessWidget {
  const NewPostCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Create Post"),
      ),
    );
  }
}
